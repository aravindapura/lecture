// Deprecated local entrypoint.
// This project is configured for Vercel deployment.
// Use `vercel dev` locally and deploy with `vercel --prod`.
console.log("Use `vercel dev` for local development. API endpoint: /api/translate");
